#nullable enable

namespace SefazSp.Epat.Infrastructure.Workflow.Elsa.Deat0050;

/// <summary>Estímulo do bookmark INICALC (evento externo), correlacionado por PROCESS_ID.</summary>
public sealed record InicalcStimulus(string CorrelationKey);
